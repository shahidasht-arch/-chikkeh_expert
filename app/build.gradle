signingConfigs {
    release {
        storeFile file(System.getenv("STORE_FILE") ?: "chikkeh-release.keystore")
        storePassword System.getenv("KEYSTORE_PASSWORD")
        keyAlias "chikkeh"
        keyPassword System.getenv("KEY_PASSWORD")
    }
}
